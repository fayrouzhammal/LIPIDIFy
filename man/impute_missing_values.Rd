% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{impute_missing_values}
\alias{impute_missing_values}
\title{Impute Missing Values in a Lipidomics Data Matrix}
\usage{
impute_missing_values(data_matrix, method = "half_min", k = 5L, seed = 42L)
}
\arguments{
\item{data_matrix}{Numeric matrix with samples as rows and lipids as
columns.}

\item{method}{Imputation method. One of \code{"half_min"} (default),
\code{"min"}, \code{"zero"}, \code{"mean"}, \code{"median"},
\code{"knn"}. See \code{\link{get_imputation_descriptions}} for details.}

\item{k}{Integer. Number of nearest neighbours for \code{"knn"}
(ignored for other methods). Default \code{5}.}

\item{seed}{Integer. Random seed for reproducibility when \code{method =
"knn"}. Default \code{42}.}
}
\value{
Imputed numeric matrix of the same dimensions as
  \code{data_matrix}.
}
\description{
Replaces \code{NA} values using the chosen strategy. Imputation should be
applied \strong{before} normalisation so that missing values do not bias
per-sample scaling factors.
}
\examples{
m <- matrix(c(1000, NA, 3000, NA, 500, 1500), nrow = 2)
impute_missing_values(m, method = "half_min")
}
